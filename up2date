#! /usr/bin/env bash

PLATFORM=`uname`

case $PLATFORM in
    Darwin)
        brew update
        brew upgrade
        brew cask upgrade
        brew cleanup
        ;;
    Linux)
        sudo apt update
        sudo apt upgrade -y
        sudo apt autoremove
        ;;
esac

